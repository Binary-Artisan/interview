import React from "react";
// Create a simple contact form that allows the user to input their name, email, phone number, a comment, and upload an image (jpg, png, svg only)

const component = (props) => {};

export default component